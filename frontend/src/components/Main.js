import './Main.css';
import {useState} from 'react';

const Main = () => {
    const [showMusic, setShowMusic] = useState(false);

    return (
        <div className="main-home">
            
        </div>
    );
}

export default Main;